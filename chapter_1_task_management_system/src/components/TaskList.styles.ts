import { makeStyles } from "@fluentui/react-components";

export const useTaskListStyles = makeStyles({
  main: {
    padding: "16px",
  },
  section: {
    width: "fit-content",
    display: "flex",
    flexWrap: "wrap",
    gap: "16px",
  },
});
